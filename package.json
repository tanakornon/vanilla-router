{
  "name": "vanilla-router",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "path-to-regexp": "^6.2.0"
  },
  "devDependencies": {
    "typescript": "^4.5.5"
  }
}
